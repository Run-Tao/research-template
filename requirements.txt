# ============================================================
# 运行时依赖（宽松版本，便于快速安装）
# 安装：pip install -r requirements.txt
# 完整可复现环境：使用 environment.yml (conda) 一键创建
# 注意：请保持与 pyproject.toml 的 [project.dependencies] 同步
# ============================================================

numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
matplotlib>=3.8
seaborn>=0.13
PyYAML>=6.0
tqdm>=4.66

# ---- 深度学习框架（按需启用，不要全部安装）----
# torch>=2.2
# torchvision>=0.17
# transformers>=4.38
# datasets>=2.17

# ---- 论文图表增强（可选）----
# adjustText>=1.1
